import 'package:flutter/cupertino.dart';

class StyledLoadingIndicator extends StatelessWidget {
  const StyledLoadingIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const CupertinoActivityIndicator();
  }
}
